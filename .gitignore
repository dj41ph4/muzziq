# dependencies
/node_modules
/engine/node_modules
/resolver/node_modules

# runtime data — never committed (real music library / config / secrets)
/.muzzik-data
/engine/.muzzik-data

# next.js
/.next/
/out/
next-env.d.ts

# production / installers
/build
/dist

# misc
.DS_Store
Thumbs.db
*.pem

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# AI assistant local config — never committed, no AI trace in this repo (règle absolue)
.claude/
/les règles opérationnelles du projet
.cursor/
.windsurf/
.cline/
.aider*
AGENTS.md
.opencode/

