# dependencies
/node_modules
/engine/node_modules
/resolver/node_modules

# runtime data — never committed (real music library / config / secrets)
/.muzziq-data
/engine/.muzziq-data

# next.js
/.next/
/out/
next-env.d.ts

# production / installers
/build
/dist

# misc
.DS_Store
Thumbs.db
*.pem

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# AI assistant local config — never committed, no AI trace in this repo (règle absolue)
.claude/
/CLAUDE.md
.cursor/
.windsurf/
.cline/
.aider*
AGENTS.md
.opencode/

